#include <iostream>
using namespace std;

int main() {
	int t;
	cin>>t;
	while(t--){
	    int a;
	    cin>>a;
	    if(a>=7)
	    cout<<"HEAVY"<<endl;
	    else if(a<3)
	    cout<<"LIGHT"<<endl;
	    else if(a>=3)
	    cout<<"MODERATE"<<endl;
	    
	}
	return 0;
}
